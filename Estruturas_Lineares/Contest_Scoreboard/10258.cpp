#include <bits/stdc++.h>

using namespace std;

class Result{
    public:
        int contestant;
        int problem;
        int time;
        char type;
};

int main(){

    int tc;
    string values;
    scanf("%d", &tc);

    while(tc--){

        // Result result;
        //
        // while(scanf("%d %d %d %d", &result.contestant, &result.problem, &result.time, &result.type), ){
        //
        // }


    }

    return 0;
}
